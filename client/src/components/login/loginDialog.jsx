import { Dialog } from '@mui/material'

const LoginDialog = ({ open }) => {
    return (
        <Dialog open={open}>
            This is dialog box
        </Dialog>
    )
}

export default LoginDialog